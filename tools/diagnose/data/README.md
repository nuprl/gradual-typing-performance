diagnosis data
===

Data files should contain a list:

    (CONFIG* ROW ...)

Where CONFIG* is a list of all configurations and:

    ROW  := (TAG DATA)
    TAG  := Symbol
    DATA := (Listof Number)

